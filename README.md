# speech-to-text-explr

## Dependencies

The repo main repo needs to be followed to have the SPARQL endpoint running.

```
conda create --prefix ./env-spchTxt python=3.1
conda activate env-spchTxt
pip install -r requirements.txt
```